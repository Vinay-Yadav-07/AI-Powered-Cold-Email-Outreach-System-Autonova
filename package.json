{
  "name": "autonova-dashboard-fullstack",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm ci --include=dev --prefix frontend && npm run build --prefix frontend && npm ci --omit=dev --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "npm@10.9.3"
}
